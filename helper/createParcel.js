
module.exports = (items, receiver) => {
  const promise = items.map(async (item) => {
    const {
      variant_id,
      title: parcel_name,
      quantity,
      grams,
    } = item
    
  })
  return Promise.all[promise]
}
